name: Apply v0.4.1 beta 4 feedback

on:
  push:
    branches: [main]
    paths:
      - 'scripts/hotfix_v041_beta4.py'
      - '.github/workflows/hotfix-v041-beta4.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  hotfix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply feedback changes
        run: python3 scripts/hotfix_v041_beta4.py
      - name: Install dependencies
        run: npm ci
      - name: Validate
        run: npm run check
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server-v4.mjs src/AppV4.jsx src/styles.css tests/workflow-v4.test.mjs package.json
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "fix: immediate AO scan and raw numeric prices"
          git push
